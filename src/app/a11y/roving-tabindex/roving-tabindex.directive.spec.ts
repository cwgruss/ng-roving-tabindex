import { RovingTabindexDirective } from './roving-tabindex.directive';

describe('RovingTabindexDirective', () => {
  it('should create an instance', () => {
    const directive = new RovingTabindexDirective();
    expect(directive).toBeTruthy();
  });
});
